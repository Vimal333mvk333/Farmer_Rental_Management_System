<!DOCTYPE html>
<html>
<head><title>Farmer Details</title></head>
<body>
<h2>{{ farmer['name'] }} - Details</h2>
<p>Contact: {{ farmer['contact'] }}</p>
<p>Balance: {{ farmer['balance'] }}</p>

<h3>Payment History</h3>
<ul>
{% for payment in payments %}
<li>{{ payment['date'] }} - {{ payment['amount'] }}</li>
{% endfor %}
</ul>

<h3>Add Payment</h3>
<form method="post" action="{{ url_for('add_payment', farmer_id=farmer['id']) }}">
    Amount: <input type="number" name="amount" step="0.01" required><br>
    Date: <input type="date" name="date" required><br>
    <button type="submit">Add Payment</button>
</form>
<a href="{{ url_for('index') }}">Back to Farmers List</a>
</body>
</html>
